import React from 'react';
import { StudentSupportPage } from '../student/StudentSupportPage';

// Re-using the generic support component for the prototype.
// The FAQ content would be tailored to affiliates in a real application.
export const AffiliateSupportPage: React.FC = () => {
    return <StudentSupportPage />;
};